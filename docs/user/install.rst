Installation
============

To install pyznuny, run this command in your terminal:

.. code-block:: console

    $ pip install pyznuny

This will install pyznuny in either your global machine python environment or your virtual environment if you have it activated.

You can also install it in your UV virtual environment by using:

.. code-block:: console

    $ uv add pyznuny